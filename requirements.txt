# Philosopher Engine Training Pipeline - Requirements
# Install with: pip install -r requirements.txt

# ---- Phase 1: Corpus Assembly ----
requests>=2.31.0

# ---- Phase 2: Text Extraction ----
PyMuPDF>=1.23.0          # fitz - PDF extraction

# ---- Phase 3-4: Cleaning & Formatting ----
# (uses stdlib only: re, json, unicodedata, hashlib, pathlib)

# ---- Phase 5: CPT Training ----
# NOTE: Install PyTorch separately for your CUDA version:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
transformers>=4.40.0
accelerate>=0.27.0
datasets>=2.18.0
wandb>=0.16.0
bitsandbytes>=0.43.0
# flash-attn>=2.5.0       # Install separately: pip install flash-attn --no-build-isolation
deepspeed>=0.14.0
sentencepiece>=0.1.99
protobuf>=4.25.0

# ---- Phase 5 (QLoRA option): ----
peft>=0.10.0
trl>=0.8.0

# ---- Phase 6: SFT Data Generation ----
anthropic>=0.25.0         # Claude API
openai>=1.14.0            # GPT-4 API
google-generativeai>=0.5.0  # Gemini API
z3-solver>=4.12.0         # Formal verification

# ---- Phase 8: Evaluation ----
# (uses transformers + torch from above)

# ---- Utilities ----
pyyaml>=6.0
tqdm>=4.66.0
